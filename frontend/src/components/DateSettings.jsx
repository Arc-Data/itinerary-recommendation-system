import Modal from "./Modal"

const DateSettings = ({onClose}) => {

    return (
        <Modal onClose={onClose}>
            <div>Something</div>
        </Modal>
    )
}

export default DateSettings